class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        d={nums[0]:0}
        for i in range(1,len(nums)):
            rem=target-nums[i]
            if rem in d:
                return d[rem],i
            else:
                d[nums[i]]=i
